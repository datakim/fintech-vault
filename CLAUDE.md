# CLAUDE.md — Fintech Vault 블로그 작업 가이드

이 저장소는 **AstroPaper v6 기반 개인 테크 블로그**다. 배포는 Vercel(`main` 브랜치에 push하면 자동 빌드·배포). 라이브: https://fintech-vault.vercel.app

이 파일은 "주제를 주면 글을 써서 배포까지" 하는 작업의 규칙을 담는다. 아래 규칙을 지키면 매번 같은 품질로 글이 올라간다.

---

## 0. 저자가 글을 요청하는 방식

저자는 보통 이렇게 말한다:

- "오늘 글은 아래 내용으로 써줘" + (유튜브 정리 메모 / 기사 초안 / 러프한 노트)
- "이 주제로 하나 써줘"

그러면 아래 **전체 워크플로우**를 처음부터 끝까지 수행한다. 중간에 저자 확인을 기다리지 말고, 초안 → 파일 생성 → 빌드 검증 → 커밋·푸시 → 라이브 확인까지 한 번에 진행한 뒤 결과를 보고한다. (톤이나 팩트에 대한 판단이 갈리면 그때만 물어본다.)

저자가 준 내용은 "재료"다. 그대로 붙여넣지 말고 **이 블로그의 목소리로 다시 쓴다**(아래 톤 규칙). 팩트가 불확실하면 표시하거나 완화한다.

---

## 1. 전체 워크플로우

1. **초안 작성** — 저자가 준 재료를 이 블로그 톤으로 재구성. 새 파일 `src/content/posts/<slug>.md` 생성.
2. **`**` 렌더링 함정 점검** (아래 3번, 매우 중요) — 어기면 화면에 `**`가 그대로 노출된다.
3. **이미지 처리** (아래 5번) — 있으면 `public/`에 넣고 `![](/파일명)`으로 참조.
4. **빌드 검증** — `pnpm build` (또는 `npm run build`)로 로컬 빌드가 깨지지 않는지 확인. 최소한 `pnpm astro check`.
5. **커밋 & 푸시** — `git add` → `git commit` → `git push`. Vercel이 자동 배포.
6. **라이브 확인** — 40~90초 대기 후 `https://fintech-vault.vercel.app/posts/<slug>/`가 뜨는지, 이미지·수식·목록 렌더링이 정상인지 확인.
7. **보고** — 글 URL, 반영 내용, 확인이 필요한 부분(이미지/팩트)을 저자에게 요약.

> Claude Code는 로컬 레포에서 직접 작업하므로 예전처럼 "격리된 클론에서 푸시"할 필요가 없다. 그냥 이 레포에서 커밋·푸시하면 된다.

---

## 2. Frontmatter 템플릿

```yaml
---
title: "글 제목 — 부제는 콜론으로"
description: "검색 결과·미리보기에 뜨는 2~3문장 요약. 핵심 수치나 결론을 담아 클릭 유도."
pubDatetime: 2026-06-17T03:00:00Z   # 실제 작성일(UTC). 오늘 날짜는 반드시 `date -u`로 확인.
tags:
  - financial-ai
  - fintech
---
```

규칙:
- **`pubDatetime`은 실제 작성일**로 넣는다. 오늘 날짜는 추측하지 말고 셸에서 `date -u '+%Y-%m-%dT%H:%M:%SZ'`로 확인. (표시 시간대는 Asia/Seoul.) 미래 날짜로 넣으면 예약 글로 숨겨진다.
- 같은 날 여러 글이면 시간(HH)만 다르게.
- **slug(파일명)** 은 영문 소문자·하이픈. 예: `alipay-ah-bao.md`, `e-cny-evolution.md`, `fds-stripe-radar.md`.
- **tags**: 아래 자주 쓰는 태그에서 고른다(새 태그는 꼭 필요할 때만). 기존 태그: `financial-ai`, `fintech`, `ai-agent`, `payments`, `engineering`, `agentic-commerce`, `llm`, `ai-coding`, `claude-code`, `alipay`, `china`, `super-app`, `cbdc`, `developer-tools`, `harness`, `machine-learning` 등.
- FDS(사기탐지) 시리즈 글이면 제목 앞에 `[FDS]`를 붙인다.

---

## 3. ⚠️ `**` 볼드 렌더링 함정 (가장 자주 실수하는 지점)

CommonMark 규칙상 **닫는 `**` 바로 뒤에 한글(또는 문자)이 붙고, 그 앞이 닫는 괄호 `)` 나 따옴표면 볼드 처리가 안 되고 `**`가 그대로 화면에 노출된다.**

깨지는 형태 (❌):
```
**도메인 불변성(domain invariant)**이다      → )** 뒤 한글
**"돈을 다루는 깊이"**다                      → "** 뒤 한글
```

고친 형태 (✅) — 닫는 `**`를 한글 단어 끝으로 옮기거나, 괄호/따옴표를 볼드 밖으로:
```
**도메인 불변성**(domain invariant)이다
"**돈을 다루는 깊이**"다
```

**새 글을 저장한 직후 반드시 이 패턴을 grep으로 검사한다:**
```bash
grep -nE '\)\*\*[가-힣]|"\*\*[^"]*\*\*[가-힣]' src/content/posts/<slug>.md
```
매치가 나오면 위 방식으로 전부 고친다. 매치가 없으면 통과.

---

## 4. 톤 & 목소리 (매우 중요 — "AI가 쓴 글" 느낌을 피한다)

기준이 되는 글: `alipay-ai-payment-era.md`, `wechat-ai-strategy.md`, `e-cny-evolution.md`. 새 글은 이 톤에 맞춘다.

- **평서체("~이다 / ~한다").** "~습니다/~합니다" 존댓말 금지. 저자가 준 재료가 존댓말이어도 평서체로 변환한다. (검사: `grep -E '습니다|입니다|합니다' src/content/posts/<slug>.md` → 나오면 고친다.)
- **요약을 인용 박스로 남발하지 않는다.** 특히 아래 패턴을 반복하지 말 것:
  - "X가 아니라 Y다." + `> 인용 블록` + "이것이 …의 핵심이다." 라는 3단 세트.
  - 한 편에 이런 콜아웃이 5~6번 나오면 기계적으로 읽힌다. 요약은 도입부 `> **핵심 요약:**` 한 곳에 모으고, 본문에서는 문장 안 **굵게**나 짧은 마무리 문장으로 흘려보낸다.
- **마무리를 한 줄 슬로건으로 억지 정리하지 않는다.** "본질은 바로 여기에 있다 / 은행을 옮긴 게 아니라…" 식의 대구 한 줄로 끝내지 말고, 자연스러운 두어 문장으로 닫는다.
- **개인 의견 섹션**은 "## N. 개인적인 견해"로 쓴다. ("핵심 인사이트", "인사이트 1/2/3" 식 나열 금지.)
- **팩트와 해석을 분리**한다. 불확실한 수치·주장은 "보도 기준", "발표 기준", "…일 가능성이 크다", "아직 지켜봐야 한다"로 낮춘다. 확정적 단정("완전히 대체했다", "모든 거래가 기록된다")을 피한다.
- 저자가 직접 인터뷰/취재한 게 아니면 "현지 언론 보도에 따르면", "…라는 취지의 사례가 소개되어 있다"처럼 출처를 명확히.
- 헤더·목록은 필요한 만큼만. 자연스러운 문단(산문) 흐름을 우선한다.

### 시리즈 내부 링크
관련 글이 있으면 본문·하단에서 내부 링크로 엮는다(SEO·회유에 도움). 형식: `[제목](/posts/<slug>/)`. 하단에 "함께 보면 좋은 글:" 목록으로 2~3개.

### 출처 표기
- 인라인 인용은 `([매체명](URL))` 형식. 예: `…라고 설명한다. ([Stripe](https://…))`
- 하단에 `## 참고 자료` 섹션으로 링크 목록.
- 사실 기반 글이면 맨 끝에 이탤릭으로 작성 기준 노트: `_본 글은 YYYY년 M월 D일 기준 공개 보도와 발표 자료를 바탕으로 작성했다. …_`

---

## 5. 이미지 처리

- 이미지는 **`public/`** 폴더에 둔다. 마크다운에서는 루트 경로로 참조: `public/foo.png` → `![대체텍스트](/foo.png)`.
- 저자가 "my-blog에 올렸어" 또는 "Downloads에 뒀어"라고 하면 그 위치에서 `public/`로 복사한 뒤 참조한다. (저자가 레포 루트에 두는 경우가 잦다 — `find . -maxdepth 1 -iname '*.png' -o -iname '*.jpg'`로 찾는다.)
- 이미지 아래에 이탤릭 캡션 + 출처: `_설명 문장. (출처: …)_`
- 이미지 파일이 아직 없으면, 본문에 넣을 자리를 명확한 주석으로 남기고("`<!-- 이미지: … / public/xxx.png 저장 후 ![](…) 활성화 -->`") 저자에게 파일을 어디에 두면 되는지 알려준다. **주석은 화면에 안 보이므로**, 실제 이미지가 필요하면 반드시 파일을 받아 활성화해야 한다.

> 채팅에 인라인으로 붙여진 이미지는 파일로 추출되지 않는다. 저자가 파일로 저장해줘야 삽입할 수 있다.

---

## 6. Books 코너 (책 연습문제 해답 등)

- 블로그 글(`/posts/`)과 분리된 별도 섹션. 파일: `src/content/books/<책폴더>/<slug>.md`, 노출: `/books/<책폴더>/<slug>`.
- 같은 책의 챕터는 frontmatter `bookTitle`을 동일하게 두면 `/books`에서 한 그룹으로 묶인다.
- frontmatter 예: `bookTitle: "단단한 머신러닝"`, `tags: [books, 단단한-머신러닝]`.
- 템플릿: `src/content/books/dandanhan-ml/_template.md` 참고(파일명 앞 `_`는 빌드 제외).

## 수식 (KaTeX)
- 수식은 이미 설정돼 있다. 인라인 `$...$`, 디스플레이 `$$...$$`. `\mathfrak`, `\binom`, `\sum`, `\text{한글}` 등 정상 렌더링됨. 그대로 쓰면 된다.

---

## 7. 배포·검증 명령

```bash
# 오늘 날짜 확인 (pubDatetime용)
date -u '+%Y-%m-%dT%H:%M:%SZ'

# 빌드 검증 (푸시 전)
pnpm build          # 전체 빌드 (astro check 포함). 시간 없으면 최소 `pnpm astro check`

# 커밋 & 푸시 → Vercel 자동 배포
git add -A
git commit -m "Add post: <영문 요약>"
git push

# 라이브 확인 (약 40~90초 후)
# https://fintech-vault.vercel.app/posts/<slug>/
```

### 의존성을 추가할 때만 주의
이 프로젝트는 **pnpm** 기반이라 `pnpm-lock.yaml`이 커밋돼 있다. 새 npm 패키지를 추가하면 `package.json`만 고치지 말고 **`pnpm install`로 lockfile도 함께 갱신**해서 커밋해야 한다. (안 하면 Vercel이 `frozen-lockfile` 에러로 빌드 실패.) 글만 쓸 때는 해당 없음.

---

## 8. 이미 되어 있는 것 (건드리지 말 것)

- **SEO**: sitemap, robots.txt, canonical, OG(+`og:locale=ko_KR`), Twitter Card, `BlogPosting`/`WebSite` JSON-LD, Google/Naver 검증 메타 — 전부 설정됨. Naver 서치어드바이저·GSC 등록 완료.
- **OG 이미지 한글 폰트**(Noto Sans KR), **Pretendard 본문 폰트**, **KaTeX**, **Books 컬렉션** — 모두 구성됨.
- 새 글을 쓸 때 이 인프라는 자동으로 적용된다. 별도 작업 불필요.

---

## 9. 새 글 체크리스트 (요약)

- [ ] `date -u`로 오늘 날짜 확인 → `pubDatetime`에 반영
- [ ] `src/content/posts/<영문-slug>.md` 생성, frontmatter 채움(title/description/pubDatetime/tags)
- [ ] 저자 재료를 **평서체 + 블로그 톤**으로 재작성 (콜아웃 남발·슬로건 마무리 금지)
- [ ] 팩트 불확실한 부분 완화, 출처 인라인 + 하단 참고 자료
- [ ] 관련 글 내부 링크 2~3개
- [ ] **`**` 깨짐 grep 검사** 통과
- [ ] 존댓말 grep 검사 통과
- [ ] 이미지 있으면 `public/`에 넣고 `![](/…)` + 캡션
- [ ] `pnpm build` 통과
- [ ] 커밋·푸시 → 라이브 확인 → 저자에게 URL·요약 보고
